@model IEnumerable<Stark.Models.Cars>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Plate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Plate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LicenceId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LicenceId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LicenceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
